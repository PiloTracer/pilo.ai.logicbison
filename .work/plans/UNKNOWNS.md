# UNKNOWNS - planning registry

> **This is a template file.** In your adopter repo it is created by **`@plan-foundation`** (P0), extended by **`@plan-master`** (planning gaps), and appended by **`@code-implementation`** (`U*` rows when tasks become blocked). In this framework repo it stays as a demo skeleton.

**Updated:** 2026-07-05 · **Maintained by:** plan-foundation / plan-master

| ID | Question / blocker | Blocks | Owner | Status |
|----|-------------------|--------|-------|--------|
| U1 | Cross-skill same-file anchor hygiene: repaired 2026-07-06 across `code-implementation`, `code-repair`, `db-migration`, `feature-spec`, `plan-master`, `plan-repair`, `plan-verify`, `session-control` (explicit `<a id>` tags + `reference.md` cross-links). All nine trimmed skills now in `ANCHOR_CLEAN` hard-fail set. | — | owner | Closed |
| U2 | `plan-foundation` greenfield protocol step ordering: registries must exist before `p0-probe` records into them — fixed in greenfield protocol (step 3 creates `ASSUMPTIONS`/`RISK_REGISTRY`/`UNKNOWNS` before step 4 `p0-probe`) | GATE p0 / greenfield protocol clarity | owner | Closed |
