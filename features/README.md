# Features (pointer)

**Feature SPECs live in [`.work/features/`](../.work/features/)**, not under `.ai/`.

Template: [`.ai/templates/work/features/example-slug/`](../templates/work/features/example-slug/)

Binding **rules** for SPECs remain in `.work/standards/*FEATURE_STANDARD*` (generated by `@plan-foundation greenfield`). **Artifacts** (SPEC files, amendments, changelogs) live under `.work/features/`.

| Action | Command |
|--------|---------|
| Create SPEC | `@feature-spec create - <slug>` |
| Review SPEC | `@feature-spec review - .work/features/<slug>/YYYYMMDD-SPEC.md` |

Portable skills reference `{FEATURE_SPEC_ROOT}` — see `.cursorrules` placeholder map.
